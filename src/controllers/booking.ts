import { Request, Response } from "express";

const get_available_slots = async (req: Request, res: Response) => {};

const book_an_appointment = async (req: Request, res: Response) => {};

export { get_available_slots, book_an_appointment };
